version: '3'

services:
  php:
    container_name: c-sf5
    build:
      context: ./
      dockerfile: ./docker/php/Dockerfile      
    volumes:
      - ./:/var/www/html
      - ./docker/php/xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini:ro
    ports:
    - 5001:80

    